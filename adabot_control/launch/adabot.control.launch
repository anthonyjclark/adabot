<launch>

  <!-- Load joint controller configurations from YAML file to parameter server -->
  <rosparam file="$(find adabot_control)/config/adabot.control.yaml" command="load"/>

  <!-- load the controllers -->
  <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false"
    output="screen" ns="/adabot" args="front_left_wheel_joint_velocity_controller
                                       front_right_wheel_joint_velocity_controller
                                       back_left_wheel_joint_velocity_controller
                                       back_right_wheel_joint_velocity_controller
                                       joint_state_controller"/>

  <!-- convert joint states to TF transforms for rviz, etc -->
  <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher" respawn="false" output="screen">
    <remap from="/joint_states" to="/adabot/joint_states" />
  </node>

</launch>
